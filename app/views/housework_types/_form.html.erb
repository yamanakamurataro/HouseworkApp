<%= form_for(@housework_type) do |f| %>
  <% if @housework_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@housework_type.errors.count, "error") %> prohibited this housework_type from being saved:</h2>

      <ul>
      <% @housework_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
